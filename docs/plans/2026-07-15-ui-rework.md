---
author: orchestrator
---
# UI Rework & System Prompt Improvement Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Improve UI styling (glass effect, terminal emulator, clear command) and fix system prompt enforcement so quarks follow superpowers rules.

**Architecture:** 
- Rework glass layer styling using actual translucent gradients (`rgba` with alpha).
- Port or implement a PTY/VTE terminal emulator in `hadron-chamber` to handle interactive shell inputs.
- Add `/clear` command to archive and truncate `field.jsonl`.
- Improve system prompts to make invariants and superpower rules prominent for ACP quarks.

**Tech Stack:** Rust, GPUI, agent-client-protocol

## Global Constraints
- Stage files by explicit path. Never use git add -A or git add .
- Run full gate `cargo test --workspace --features gui` to verify each step.

---

### Task 1: Completions HoverCard Rework (Completed)
**Files:**
- Modify: `crates/hadron-chamber/src/app.rs`
- [x] Reworked popup overlay using absolute positioning bottom-above the input.

### Task 2: Tabs and Fields Polish (Completed)
**Files:**
- Modify: `crates/hadron-chamber/src/app.rs`, `crates/hadron-chamber/src/theme.rs`
- [x] Restyled input fields and added window glint top sheen.

### Task 3: Glass Effect Transparency Improvement (Completed)
**Files:**
- Modify: `crates/hadron-chamber/src/theme.rs`
- Test: `cargo test -p hadron-chamber --features gui`

- [x] **Step 1: Update glass surface gradient with alpha channels**
  Modify `theme::glass_surface()` in `crates/hadron-chamber/src/theme.rs` to use `rgba` gradients so the panel is translucent over `window_glint()`.
  ```rust
  pub fn glass_surface() -> Background {
      linear_gradient(
          180.0,
          linear_color_stop(rgba(0x09090b80), 0.0), // 50% opacity top sheen
          linear_color_stop(rgba(0x09090bbf), 1.0), // 75% opacity base
      )
  }
  ```
- [x] **Step 2: Check compilation**
  Run: `cargo check -p hadron-chamber --features gui`
  Expected: Clean compilation.
- [x] **Step 3: Commit**
  Run: `git add crates/hadron-chamber/src/theme.rs && git commit -m "feat(theme): improve glass transparency with alpha gradient"`

### Task 4: Autoscroll Bug (Completed)
**Files:**
- Modify: `crates/hadron-chamber/src/app.rs`
- [x] Deferred `scroll_to_bottom` to `window.on_next_frame` on Shift+Enter text changes.

### Task 5: Real PTY/VTE Terminal Emulator
**Files:**
- Modify: `crates/hadron-chamber/src/sys.rs`, `crates/hadron-chamber/src/app.rs`
- Test: `cargo test -p hadron-chamber`

- [x] **Step 1: Add PTY session runner to sys.rs**
  Integrate real interactive PTY reading/writing (e.g. using `portable-pty` or spawning a sub-shell and handling stdout/stdin streams asynchronously).
- [ ] **Step 2: Update UI rendering to draw interactive output**
  Handle keyboard input byte-by-byte instead of only on Enter.
- [ ] **Step 3: Commit**
  Run: `git commit -m "feat(chamber): implement real interactive PTY terminal"`
- [ ] **Step 4: Remove Red Risk banner from the terminal section**
  Remove the `risk_notice` warning banner from the top of the terminal tab.

### Task 6: Backstop Configuration & Mode Exemption (Completed)
**Files:**
- Modify: `crates/hadron-gluon/src/engine.rs`, `crates/hadron-lattice/src/team.rs`
- [x] Added backstop limit configuration in `team.json` and exempted Bypass mode.

### Task 7: Clear Command Implementation
**Files:**
- Modify: `crates/hadron-chamber/src/app.rs`
- Test: `cargo test`

- [ ] **Step 1: Implement `/clear` slash command**
  Add `/clear` command to archive current `field.jsonl` in `.hadron/sessions/{session_id}/field.jsonl` and empty `.hadron/field.jsonl`.
- [ ] **Step 2: Propagate to active quarks**
  Send a reset notification or command to active seats.
- [ ] **Step 3: Commit**
  Run: `git commit -m "feat(chamber): implement /clear command to archive and truncate field"`

### Task 8: Research and Fix Quark System Prompts
**Files:**
- Modify: `crates/hadron-gluon/src/adapter/prompt.rs`, `crates/hadron-gluon/src/adapter/acp.rs`
- Test: `cargo test`

- [ ] **Step 1: Analyze how prompts are delivered to ACP**
  Check if Claude Code ignores rules due to prompt formatting.
- [ ] **Step 2: Strengthen system prompt directives**
  Make invariants and superpowers rules more prominent.
- [ ] **Step 3: Commit**
  Run: `git commit -m "fix(gluon): strengthen prompt rules for ACP quarks"`

### Task 9: Remove Redundant Top Command Palette
**Files:**
- Modify: `crates/hadron-chamber/src/app.rs`
- Test: `cargo test`

- [ ] **Step 1: Remove command palette overlay and keybindings**
  Remove `palette_overlay`, `TogglePalette` action, `palette_open` state, and its references.
- [ ] **Step 2: Commit**
  Run: `git commit -m "feat(chamber): remove redundant command palette"`
