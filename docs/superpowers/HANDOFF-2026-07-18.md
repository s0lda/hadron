# Overnight autonomous session — handoff (2026-07-18)

You asked me to implement the two taxonomy follow-ups, three UI fixes, and plan+implement the permissions/extensibility spec, autonomously, "one step at a time." Here's everything, honestly. **Nothing is merged, nothing was pushed, no daemon was run, your live config was not touched by me. Everything is on stacked review-branches and green.**

## TL;DR

- **4 workstreams fully implemented + reviewed + green.** 1 documented WIP. 1 security feature deliberately left as a reviewed plan (not half-built).
- Every code change went through the two-tier review (fresh implementer → adversarial reviewer per task, then a whole-branch review). The reviews repeatedly caught real cross-task defects — each was fail-closed or inert, never shipped broken.
- **Full test suite: 472 passing, 0 failing** at the tip (`cargo test --workspace --features gui`), up from 413 at the start.
- The GUI changes are **traced-correct + logic-unit-tested but NOT visually verified** — this box is headless (WSL2, no window paints). Those need your eyes. Marked below.

## The branch stack (linear — each stacked on the previous; the tip has everything)

```
feat/transport-first-taxonomy   (WS #2, was already done before tonight; + your permissions spec commits)
  └─ feat/custom-cli-transport   (WS1)  @ eabf6a8   ✅ reviewed
      └─ feat/prompt-bloat-trim   (§5)  @ cc1f1ca   ✅ reviewed
          └─ feat/ui-fixes        (UI)  @ e58ed81   ✅ reviewed
              └─ feat/role-routing (§4) @ 1d5e8c4   ✅ reviewed
                  └─ feat/sdk-adapter-wip     @ f6204c2   📄 docs-only WIP
                      └─ feat/permissions-gating @ e28ec46 📄 specs+plans+questions
```

Review each branch's own diff (`git diff <parent>..<branch>`), or the whole thing at the tip. Specs/plans are in `docs/superpowers/specs/` and `docs/superpowers/plans/`; per-workstream SDD ledgers (with every decision + finding) are in `.superpowers/sdd/*.md`.

---

## What shipped

### WS1 — Custom CLI transport (`feat/custom-cli-transport`) ✅

Made `Transport::Cli` generic/config-driven. One `CliQuark` adapter driven by a serializable `CliSpec` (program, args, prompt channel, model flag, resume, timeout, posture, argv-guard). **Deleted the bespoke `claude.rs` and `agy.rs`** — `agy`'s hardened behavior (E2BIG argv guard, `--print-timeout`, `--continue` resume, posture) is preserved byte-for-byte as `CliSpec::agy()` and proven by porting agy's entire test module unchanged. Claude is ACP-only (your call). Added a "Custom CLI" add-quark path in Settings, and hardened `validate_quark_id` to reject path/git-ref-unsafe ids (`/`, `..`, etc.) — the wizard is the first UI feeding free-typed text into ids that become worktree dirs + branch refs.

- **Needs your eyes:** the Settings "Custom CLI" wizard (GUI, not run headless).

### WS4 §5 — Prompt-bloat trim (`feat/prompt-bloat-trim`) ✅

Deleted `skills::corpus()` — the ~70-80k-token full-skill-library dump into every resident (ACP) quark's prompt on every turn. Now every quark (resident or one-shot) gets only `skills::index()` (the menu) + the active skill's body. This is the lever for the ACP "Context %" filling fast. Verified the discriminating test against the real skill fixtures.

### WS3 — UI fixes (`feat/ui-fixes`) ✅ (with one honest correction)

- **Tab-swap (Issue C):** the `ToggleFocus` chat↔terminal action was **never built** — bare Tab fell through to gpui's focus-traversal. Built it on **`ctrl-\`` ** (backtick, the terminal-toggle convention); also moved `CycleMode`off`shift-tab` (which the text input silently claims for outdent) to **`f6`**.
- **Sidebar (Issue A):** roster width 245→**310**; the mode tag now renders for **every** quark (solid=override, outline=inherited) — before, default-mode quarks showed no tag at any width; and the **folded** rail now shows each quark's avatar + state dot.
- **Chat input scroll (Issue B): NO code change — the prescribed fix was a proven no-op.** I traced deeper: `enter()` already scrolls to the caret (via `replace_text_in_range`'s unconditional `scroll_to`). Your symptom is almost certainly a **stale running binary** built before that fix landed. **ACTION: rebuild the chamber and re-test Shift+Enter.** If it persists after a rebuild, it's a different cause that needs your eyes at a real window. (Details + a regression test committed to the `/tmp/gpui-component` fork `@9d9f3e18`.)
- **Needs your eyes:** all three — whether `ctrl-\`` reaches the app past the WSL2/WM layer, the 310px/folded-avatar look, and the Shift+Enter behavior after a rebuild.

### WS4 §4 — Role routing (`feat/role-routing`) ✅

Seats/cards gained `roles` + `exclusive`. `@role` mentions (e.g. `@security`) soft-prefer a matching seat (id/alias keep precedence; unmatched roles fall back softly). An `exclusive` seat is admitted **only** when a task specifically names its `@id`, a `@role` it carries, or its `@display_name` — never swept in by an `@team` broadcast — else it's filtered and the gap is reported (never stalled). Roles are plumbed onto the quark→card the same proven way `display_name` flows (the review caught that the config alone was inert without this). **Personas (`.hadron/agents/`) deferred** to share `.md`-loading machinery with the skills-loading work (see §2 spec).

- **Deferred Minor (for you):** role selection currently skips only _depleted_ seats, not the engine's _disabled_ set (the card has no `enabled` field) — a `@role` can resolve to a disabled same-role seat and report "disabled" instead of routing to an enabled peer. Fail-soft, uncommon config. Fix needs `enabled` surfaced onto `QuarkCard`.

---

## What I deliberately did NOT build (and why)

### WS2 — Native SDK adapter for agy → `docs/.../2026-07-18-sdk-adapter-wip.md` (docs only)

A real `Transport::Sdk` needs a live Gemini API client (`GEMINI_API_KEY` + network I don't have). Self-authored fixtures would only prove the code matches my _memory_ of the wire format, not the real API — "plausible-but-unrun," which violates "prove it runs." The Python bridge (`agy_acp.py`) already works. **Shipped a thorough spec+plan you execute with your key; kept the `Transport::Sdk` hard-bail honest.** Open question in the doc: is the goal a native Rust client, or just to rename `acp-agy` honestly?

### Permissions & extensibility → `feat/permissions-gating` (specs+plans+questions, NOT built)

- **§2 No-Human-Mode gating** (`...no-human-mode-gating-spec-and-plan.md`): implementation-ready plan. I did **not** build it because it edits the live security-gating path (`decide()` is runtime-consumed at `engine.rs:1073`), and the _policy_ — an LLM auto-adjudicating what would otherwise ask you — is your call, not one to switch on unattended. The plan keeps it strictly additive (Rule 4): `AskOrchestrator` fires **only** under explicit global `Bypass`; every other path stays byte-for-byte `AskHuman`; gated behind a `DO-NOT-ACTIVATE` toggle. The suspend→adjudicate→resume loop is fully testable headless with fake quarks — that's where the build effort goes when you approve.
- **§3.3 custom script tools:** **firm no-build.** Arbitrary `.py`/`.rs` compiled-and-run in the shared tree can't even be _tested_ unattended (a test executes attacker-shaped code). Spec + a hard set of trust/sandbox questions only.
- **§2-skills/persona loading** (the buildable, safe extensibility piece): spec + plan ready.

## ⚠️ DESIGN QUESTIONS I need you to answer (these gate the permissions work — more valuable than code I could guess at)

1. **LLM-approves-LLM:** is the orchestrator (an LLM the worker's request text can influence) adjudicating worker commands acceptable for your threat model? Which command classes must **always** reach a human even under global Bypass (`rm -rf`, network exfil, credential access, `git push`)?
2. **Is a human deny-list entry absolute** (the orchestrator can never override it), even in No-Human-Mode? (I recommend yes.)
3. **Script tools:** who may author them (repo `.hadron` = anyone with repo write, vs global `~/.hadron` = only you)? **No sandbox (the spec's "quarks-share-the-tree") — accepted, or must they be sandboxed?** This is the single biggest safety decision in the whole spec.
4. Allow/deny-list **storage + syntax** (`team.json` vs a new file vs field events; exact-match vs prefix/glob)?
5. **SDK adapter:** native Rust Gemini client, or just honestly rename `acp-agy`?

Answers:

1. Bypass = dangerously allow commands for orchestrator.
2. Quark need perm > Ask, Write, Auto > Orchestrator checks allow list and allows or not > Bypass orchestrator decides, if people dont wont risk, they stay in auto mode, that follows all lists
3. Each quark should carry the task on separate worktree, We should add option for user to decide Sandoxed or not (also we need option for max_exchanges in settings, not present)
4. exact match and prefix/glob, dont think native Rust exist, unless we do it.

---

## Housekeeping notes

- **Your live config was migrated by _your own_ chamber launch**, not by me — at session start `~/.hadron/team.json` was old-format; it's now the new taxonomy format (`cli-agy`, `vendor` fields). Benign and idempotent (the migration is what it's for). One thing to check: your local `.hadron/team.json` shows `acp-codex` `enabled: false` — if you didn't toggle that yourself, it's worth a look (I did not touch it; backed up `~/.hadron/team.json.autobak-*`).
- **`/tmp/gpui-component`** (your gpui fork, branch `text-mark-color`) has one extra harmless commit from me: `9d9f3e18`, a regression test pinning the input's scroll-to-caret behavior. The hadron Cargo.lock still pins the old fork commit, so your build is unaffected.
- Subagents appended a few CLAUDE.md-rule-9 lesson notes to the gitignored `.hadron/memory/index.md` (accurate, benign — left in place).
- **Do the WS3-B rebuild** before concluding the chat-scroll bug is real.
- Recommended review order: WS1 → §5 → UI → §4 (the shipped code), then the SDK WIP doc, then the permissions specs + the 5 questions above.

Everything's green, inert, and reversible. Wish I could've verified the GUI for you — that's the one thing this headless box can't do.
