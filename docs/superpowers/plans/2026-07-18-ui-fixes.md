# Chamber UI Fixes Implementation Plan (WS3)

> **For agentic workers:** REQUIRED SUB-SKILL: subagent-driven-development / executing-plans. Checkbox steps.

**Goal:** Fix three user-reported chamber UI issues: keyboard chat↔terminal switch (never built), left-sidebar width + per-quark tags + folded avatars, and chat-input scroll-to-caret on newline.

**Architecture:** Issues A & C are in-repo (`crates/hadron-chamber`). Issue B's core fix is a one-line addition in the out-of-tree gpui-component fork (`/tmp/gpui-component`, branch `text-mark-color`), surfaced in-repo as a `Cargo.lock` pin bump.

**Tech Stack:** Rust, gpui / gpui-component (GUI). cargo test. **GUI cannot be run headless — logic is unit-tested; interaction is NEEDS-EYES.**

## Global Constraints
- Baseline gate before/after: `cargo test --workspace --features gui` (full).
- INERT: cargo test/check only, never run the chamber/gluon binaries; tempdirs; don't touch live ~/.hadron.
- HONESTY: never claim a visual/interaction behaviour "works" — it's traced-correct + logic-tested; mark NEEDS-EYES in the report for anything requiring a real window.
- Keybinding rule: a new chord must be verified UNCLAIMED by the Input context — grep every `KeyBinding::new(...)` in `/tmp/gpui-component/crates/ui/src/input/state.rs` (same as `crates/gpui-component/crates/ui/src/input/state.rs`) before using it. Never use bare `Tab` or `shift-tab` for a Chamber action.
- One focused commit per task.

---

### Task 1 (Issue C): keyboard chat↔terminal toggle + fix dead shift-tab binding

**Files:** `crates/hadron-chamber/src/app/mod.rs` (actions! macro, keybindings), `crates/hadron-chamber/src/app/render.rs` (on_action handler), `crates/hadron-chamber/src/app/actions.rs` (toggle helper + test), possibly `crates/hadron-chamber/src/app/tabs.rs`.

- [ ] **Step 1: Verify a free chord.** Grep every `KeyBinding::new("...")` in `crates/gpui-component/crates/ui/src/input/state.rs`. Confirm `ctrl-\`` (ctrl+backtick) is NOT among them; if it is or is unusable, use `f6`. Record the choice + evidence in the report. Also confirm a free chord for `CycleMode` (currently `shift-tab`, which Input claims for `OutdentInline`).
- [ ] **Step 2: Write a failing unit test** for the toggle's pure logic. If `ToggleFocus` needs state-transition logic (e.g. "which focus target given the current right-rail tab"), extract it as a pure helper (mirror `cycle_chat_tab`/`cycle_inspector_tab` at `actions.rs:114-127`) and test it: when `RightRailTab::Terminal` active → target = terminal; else → target = chat (and rail switches to Terminal). Name it e.g. `toggle_focus_targets_terminal_when_terminal_tab_active`.
- [ ] **Step 3: Run — expect FAIL** (helper undefined).
- [ ] **Step 4: Implement.** Add `ToggleFocus` to the `actions!` macro (`mod.rs:77-92`). Bind the verified chord (`mod.rs:1078-1094` area). Add an `on_action` handler (near `render.rs:71-86`) using the pure helper: toggle window focus between `self.input`'s focus handle and the terminal pane's focus handle; if the terminal isn't the active right-rail tab, set it active then focus it. Move `CycleMode` off `shift-tab` to the verified-free chord; update the inaccurate comment at `mod.rs:1069-1077` to state the shift-tab/Input conflict was fixed.
- [ ] **Step 5: Run** helper test + full gate. Expect PASS.
- [ ] **Step 6: Commit** — `git commit -m "feat(chamber): keyboard toggle focus chat<->terminal; unbind shift-tab from Input's OutdentInline"`

**NEEDS-EYES (report):** whether the chord reaches the app past the WSL2/WM layer; whether focus lands where expected; chord feel/collisions.

---

### Task 2 (Issue A): sidebar width + always-render mode tag + folded avatars

**Files:** `crates/hadron-chamber/src/config.rs` (width const), `crates/hadron-chamber/src/app/widgets.rs` (`mode_tag`), `crates/hadron-chamber/src/app/render.rs` (`rail_strip` folded avatars), `crates/hadron-chamber/src/app/identity.rs` (reuse `identity_avatar`).

- [ ] **Step 1: Width.** In `config.rs:59-61`, change `default_roster_width()` 245.0 → 310.0.
- [ ] **Step 2: Write a failing test for `mode_tag`.** `mode_tag(mode, is_override)` currently returns an empty element when `!is_override`. Write a test asserting the non-override case now renders a non-empty tag (whatever pure assertion the element type allows — if the element isn't introspectable, extract the decision into a pure `fn mode_tag_should_render(is_override: bool) -> bool` or a small `enum TagStyle { Solid, Outline }` selector `fn mode_tag_style(is_override) -> TagStyle` and test THAT). Name it `mode_tag_renders_for_inherited_mode_not_only_overrides`.
- [ ] **Step 3: Run — expect FAIL.**
- [ ] **Step 4: Implement.** In `widgets.rs:435-449`, drop the `if !is_override { return empty }` early return; render the resolved `mode` for all quarks, styling override = solid and inherited = outline (per the function's doc comment). Keep `effort_tag` as-is.
- [ ] **Step 5: Folded avatars.** In `render.rs:680-724` `rail_strip()`, for `Rail::Roster`, add a per-quark loop above the pinned Settings button rendering `identity_avatar(&r.id, px(24.))` (`identity.rs:87-106`) + the status dot coloured by `theme::presence(r.state)` (mirror `widgets.rs:250-258`), for each roster row the pane already has. Keep it within the 44px `RAIL_STRIP`.
- [ ] **Step 6: Run** test + full gate. Expect PASS.
- [ ] **Step 7: Commit** — `git commit -m "feat(chamber): wider roster, mode tag on every quark, folded-rail avatars"`

**NEEDS-EYES (report):** 310px width legibility; folded avatar size/dot; truncation with long names + both tags.

---

### Task 3 (Issue B): chat-input scroll-to-caret on newline (fork fix + in-repo cleanup)

**Files:** `/tmp/gpui-component/crates/ui/src/input/state.rs` (fork, `enter()` + a test), `crates/hadron-chamber/src/app/mod.rs:760-768` (delete misdirected branch), `Cargo.lock` (pin bump), and mirror the fork edit into the in-repo reference copy `crates/gpui-component/crates/ui/src/input/state.rs` so the two stay byte-identical (the diagnosis confirmed they are today).

- [ ] **Step 1: Confirm fork state.** `git -C /tmp/gpui-component status` clean, on `text-mark-color`. (Controller confirmed: clean, HEAD f1e4d214, matches Cargo.lock.)
- [ ] **Step 2: Add a failing fork test.** In `/tmp/gpui-component/crates/ui/src/input/state.rs`, following the existing pattern at `state.rs:3320` (`test_scroll_to_eob_does_not_overshoot_safe_range`), add a `gpui::test` that builds a small-height auto_grow input, inserts enough newlines via `enter()` (shift) to overflow the visible rows, and asserts the input's `scroll_handle` offset advanced (caret row now in view). Run it in the fork: `cd /tmp/gpui-component && cargo test -p <ui-crate-name> <test>` — expect FAIL before the fix.
- [ ] **Step 3: Fix `enter()`.** In `enter()`'s `if insert_newline` block, right after the `replace_text_in_range_silent(...)` call (~state.rs:1641), add `self.scroll_to(self.cursor(), None, cx);` — mirroring `paste()` at `state.rs:2055`. Re-run the fork test → PASS.
- [ ] **Step 4: Mirror into the in-repo reference copy** `crates/gpui-component/crates/ui/src/input/state.rs` (same one-line addition) so the vendored copy doesn't drift from the fork.
- [ ] **Step 5: Commit the fork.** `git -C /tmp/gpui-component add -A && git -C /tmp/gpui-component commit -m "fix(input): scroll to cursor after newline in enter() (mirror paste)"`. Record the new fork commit SHA in the report.
- [ ] **Step 6: In-repo cleanup + pin bump.** Delete the misdirected shift branch at `crates/hadron-chamber/src/app/mod.rs:760-768` (the `chat_scrolls` scroll-to-bottom that doesn't affect the input). Then `cargo update -p gpui-component -p gpui-component-macros -p gpui-component-assets` so `Cargo.lock` repoints to the new fork commit. Confirm the workspace still builds: `cargo check --workspace --features gui`.
- [ ] **Step 7: Run the full gate** `cargo test --workspace --features gui`. Expect PASS.
- [ ] **Step 8: Commit in-repo** — `git commit -m "fix(chamber): scroll input to caret on newline (gpui-component pin bump); drop misdirected transcript-scroll branch"`

**NEEDS-EYES (report):** whether the newline visibly scrolls into view and feels smooth at a real window. **REVIEWABILITY FLAG:** the actual fix is in `/tmp/gpui-component` (ephemeral, outside the hadron repo) — the in-repo trace is the Cargo.lock pin + the mirrored reference copy. Report the fork commit SHA prominently so the user can preserve/cherry-pick it.

---

## Self-Review
- Spec Issue A → Task 2 (width, mode_tag-always, folded avatars). Issue B → Task 3 (fork enter() + cleanup + pin). Issue C → Task 1 (ToggleFocus + shift-tab fix). ✓
- Placeholder scan: chord choice + free-chord verification are steps requiring the implementer to grep actual bindings (a read, not a placeholder); the exact test assertions for GUI elements are described with fallback to a pure extracted helper where the element isn't introspectable. ✓
- Honesty: every task carries a NEEDS-EYES block; Task 3 carries the fork-reviewability flag. ✓
